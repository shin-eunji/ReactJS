const initialState = {
    openSidebar: false
}

function reducer(state= initialState, action) {
    switch (action.type) {
        default: return state;
    }
}
/*
const reducer*/

export default reducer;